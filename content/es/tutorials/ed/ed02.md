
---
title: ED-02 Gaps
math: true
toc: true
---

## Gaps de espín en sistemas de espín cuánticos 1D

En este tutorial usaremos el programa de diagonalización dispersa para calcular gaps de espín de sistemas de espín cuánticos 1D y estudiar su escalamiento de tamaño finito.

### Gap de espín de una cadena de espín-1

#### Usando la línea de comandos
El archivo de parámetros <a href="../codes/ed-02-gaps/parm2a" download>`parm2a`</a> configura la diagonalización exacta de la cadena cuántica S=1 con entre 4 y 10 sitios en el sector singlete y triplete

```
MODEL="spin"
LATTICE="chain lattice"
CONSERVED_QUANTUMNUMBERS="Sz"
local_S=1
J=1
Sz_total=0
{L=4}
{L=6}
{L=8}
{L=10}
Sz_total=1
{L=4}
{L=6}
{L=8}
{L=10}
```
    
El parámetro Sz_total restringe el número cuántico conservado Sz al valor dado. Consulte la documentación de la biblioteca de modelos para más detalles sobre cómo configurar modelos y números cuánticos conservados.
Usando la secuencia estándar de comandos, primero puede convertir los parámetros de entrada a XML y luego ejecutar la aplicación `sparsediag`:

```
parameter2xml parm2a
sparsediag --write-xml parm2a.in.xml
```
    
La evaluación de los gaps ahora puede hacerse manualmente examinando los archivos de salida, o podemos usar Python para automatizar todo el flujo de trabajo.

#### Usando Python
Para configurar y ejecutar la simulación en Python usamos el script <a href="../codes/ed-02-gaps/tutorial2a.py" download>`tutorial2a.py`</a>. Las primeras partes de este script importan los módulos requeridos, preparan los archivos de entrada como una lista de diccionarios de Python, escriben los archivos de entrada y ejecutan la aplicación

```
import pyalps
import numpy as np
import matplotlib.pyplot as plt
import pyalps.pyplot
parms = []
for l in [4, 6, 8, 10]:
    for sz in [0, 1]:
        parms.append({ 
            'LATTICE'                   : "chain lattice", 
            'MODEL'                     : "spin",
            'local_S'                   : 1,
            'J'                         : 1,
            'L'                         : l,
            'CONSERVED_QUANTUMNUMBERS'  : 'Sz',
            'Sz_total'                  : sz
        })
     
input_file = pyalps.writeInputFiles('parm2a',parms)
res = pyalps.runApplication('sparsediag',input_file)
```
    
Para ejecutar esto, escriba en su terminal `python tutorial2a.py`. Ahora tenemos los mismos archivos de salida que en la versión de línea de comandos.
A continuación cargamos los espectros de cada uno de los tamaños de sistema y sectores de espín:

```
data = pyalps.loadSpectra(pyalps.getResultFiles(prefix='parm2a'))
```
    
Para extraer los gaps necesitamos escribir algunas líneas de Python, para configurar una lista de longitudes y un diccionario de Python con la energía mínima en cada sector (L,Sz):

```
lengths = []
min_energies = {}
for sim in data:
    l = int(sim[0].props['L'])
    if l not in lengths: lengths.append(l)
    sz = int(sim[0].props['Sz_total'])
    all_energies = []
    for sec in sim:
        all_energies += list(sec.y)
    min_energies[(l,sz)]= np.min(all_energies)
```

Y finalmente hacemos un gráfico del gap en función de 1/L y luego mostramos el gráfico
    
```
gapplot = pyalps.DataSet()
gapplot.x = 1./np.sort(lengths)
gapplot.y = [min_energies[(l,1)] -min_energies[(l,0)] for l in np.sort(lengths)]  
gapplot.props['xlabel']='$1/L$'
gapplot.props['ylabel']='Triplet gap $\Delta/J$'
gapplot.props['label']='S=1'

plt.figure()
pyalps.plot.plot(gapplot)
plt.legend()
plt.xlim(0,0.25)
plt.ylim(0,1.0)
plt.show()
```

## Gap de espín de una cadena de espín-1/2

Compare el gap extrapolado de la cadena de espín-1 con el de una cadena de espín-1/2. Para ello, simplemente cambie el espín local local_S=1 a local_S=0.5 y ejecute las simulaciones (pero déles el nombre `parm2b`). El archivo de parámetros es <a href="../codes/ed-02-gaps/parm2b" download>`parm2b`</a> y el script de Python es <a href="../codes/ed-02-gaps/tutorial2b.py" download>`tutorial2b.py`</a>.

## Ambos sistemas en un solo gráfico

Para mostrar ambos gaps en un solo gráfico, ejecute el script de Python <a href="../codes/ed-02-gaps/tutorial2c.py" download>`tutorial2c.py`</a> después de ejecutar los dos primeros tutoriales.
Los gaps en función del tamaño de la red para espín 1/2 y 1 se grafican en la siguiente figura:
![](figs/spingaps.png)

## Preguntas

- ¿Cuál es el valor extrapolado del gap para un sistema infinito?
- ¿Por qué las cadenas S=1/2 y S=1 muestran comportamientos diferentes?
